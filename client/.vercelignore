# Exclude large instance .txt files from deployment
# Keep index.json and logic-solvable puzzles (small set, only 22 files)
# Use specific patterns to exclude only large .txt file directories

# Exclude .txt files from large directories only
public/instances/6x6/**/*.txt
public/instances/12x12/**/*.txt
public/instances/general/**/*.txt
public/instances/daily-puzzles/**/*.txt

# Keep logic-solvable puzzles (small set, useful for production)
# Don't exclude public/instances/logic-solvable/

# Exclude entire subdirectories that only contain .txt files
public/instances/6x6/
public/instances/12x12/
public/instances/general/
public/instances/daily-puzzles/

# Exclude root instances directory (outside client/)
instances/

# Exclude development files
node_modules/
.vscode/
*.log
.DS_Store

# Exclude build artifacts (but Vercel needs dist/, so be careful)
# dist/  # Don't exclude - Vercel needs this
build/
.vercel/

# Exclude test files
*.test.js
*.test.jsx
*.spec.js
*.spec.jsx
